@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<HR_Module_Proj.Models.MasterStaff>

<h2>Staff List</h2>
<a asp-action="StaffForm" class="btn btn-success">Add New Staff</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Staff Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var staff in Model)
        {
            <tr>
                <td>@staff.StaffName</td>
                <td>@staff.Email</td>
                <td>
                    <a asp-action="StaffForm" asp-route-id="@staff.StaffId" class="btn btn-warning btn-sm">Edit</a>

                    <form asp-action="DeleteStaff" asp-route-id="@staff.StaffId" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?');">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

